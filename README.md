# 小小星光 · 儿童作品集

一个面向儿童创作的数字作品展厅，用来温柔地收藏、展示和回顾孩子的绘画、手作与影像作品。

> 每一笔、每一次尝试和每个天马行空的想法，都值得被好好看见。

## 设计理念

“小小星光”并不以作品是否精致或写实作为标准，而是将作品视为孩子表达世界的方式。界面采用柔和留白、手帐相框、轻微错落排版和童趣色彩，让浏览体验像走进一间会慢慢长大的小小美术馆。

## 已实现功能

- **作品墙**：以错落画廊布局呈现儿童作品。
- **作品分类**：可筛选全部、绘画、手作和影像作品。
- **影像标识**：影像作品带有醒目的播放标识。
- **作品详情**：点击作品可查看日期、创作媒介、作者信息和孩子的创作话语。
- **喜欢作品**：支持在作品墙和详情页标记喜欢。
- **响应式布局**：针对桌面与手机屏幕优化展示和导航。
- **本地示例素材**：使用项目内的 SVG 插画作为示例作品，离线预览也能完整展示。

## 技术栈

- React 19
- TypeScript
- Vite
- CSS（原生响应式样式，不依赖 UI 框架）
- Lucide React 图标

## 快速开始

### 环境要求

- Node.js 20 或更高版本
- npm 10 或更高版本

### 安装依赖

```bash
npm install
```

### 本地开发

```bash
npm run dev
```

启动后，在终端提示的本地地址打开网站。默认地址通常为 `http://localhost:5173`。

### 构建生产版本

```bash
npm run build
```

构建后的静态文件会生成在 `dist` 目录中。

### 预览生产构建

```bash
npm run preview
```

## 项目结构

```text
kids-gallery/
├── public/
│   └── art/                 # 本地示例作品 SVG
├── src/
│   ├── App.tsx              # 页面、作品数据与交互逻辑
│   ├── main.tsx             # React 应用入口
│   └── styles.css           # 全局与响应式样式
├── index.html
├── package.json
└── vite.config.ts
```

## 替换为真实作品

当前作品数据集中在 `src/App.tsx` 的 `artworks` 数组中。接入真实数据时，可将每件作品扩展为以下信息：

- 作品名称、作者昵称与年龄
- 创作日期、作品分类、创作媒介
- 图片或视频地址
- 孩子的作品说明或创作故事
- 作品点赞、标签、家庭成员留言等互动数据

建议后续将作品数据迁移到后端服务或内容管理系统，并为上传内容提供家长授权、访问权限控制和隐私保护机制。

## 后续可扩展方向

- 家长上传图片和视频、自动生成封面
- 作品按孩子、年份、主题或成长阶段归档
- 家庭成员评论与共同收藏
- 私密相册、分享链接与访问密码
- 年度作品册导出和成长时间轴
- 视频播放、字幕和作品录音说明

## 可用脚本

| 命令 | 用途 |
| --- | --- |
| `npm run dev` | 启动本地开发服务 |
| `npm run build` | 执行 TypeScript 检查并构建生产文件 |
| `npm run preview` | 本地预览生产构建 |
| `npm run lint` | 运行 ESLint 检查 |